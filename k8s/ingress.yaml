apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sentinel-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sentinel-service
                port:
                  number: 8080